prefix=/usr
exec_prefix=${prefix}
includedir=${prefix}/include
libdir=${exec_prefix}/lib

Name: ion-core
Description: The library to build and compile pipelines
Version: 0.0.1

Requires:
Cflags: -I${includedir}
Libs: -L${libdir} -lion-core -lHalide -lpthread -lrt -ldl -lm
